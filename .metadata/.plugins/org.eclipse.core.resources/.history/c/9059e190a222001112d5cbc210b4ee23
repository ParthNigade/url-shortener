package com.parth.urlshortener.exception;

public class UrlNotFoundException {
	public UrlNotFoundException(String shortCode) extends RuntimeException {
        super("URL not found for short code: " + shortCode);
    }
}
